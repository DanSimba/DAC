info:
  name: Get Clientes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/clientes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
