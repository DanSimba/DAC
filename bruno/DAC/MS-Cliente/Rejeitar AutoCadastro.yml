info:
  name: Rejeitar AutoCadastro
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8081/clientes/solicitacoes/{cpf}
  body:
    type: json
    data: |-
      {
        "motivo":"teste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
